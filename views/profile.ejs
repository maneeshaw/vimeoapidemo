<p>
  ID:
  <%= user.id %><br/> Username:
    <%= user.username %><br/> Name:
      <%= user.displayName %><br/>
        <% if (user.emails) { %>
          Email:
          <%= user.emails[0].value %><br/>
            <% } %>
</p>
<a href="/logout">Log out</a>

<a href="<%= otturl%>">OTT URL</a> var request = require("request");

<div>
  <% watchlist.forEach(function(list) { %>
    <div>
      <h3><%= list.name %></h3>
      <img src="<%= list.thumbnail.medium %>" width="200" />
    </div>
    <% }) %>
</div>